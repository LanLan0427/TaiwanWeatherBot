# TaiwanWeatherBot (Discord 氣象 & 天文機器人)

這個專案包含兩個自動化機器人：**每日氣象廣播**與**NASA 天文每日一圖 (APOD)**。它們會定時抓取資料，並透過 **Google Gemini AI** 生成生動有趣的解說，最後發送到 **Discord** 頻道。

## ✨ 功能特色

### 🌤️ 台灣氣象機器人 (`weather_bot.py`)
- **資料來源**：中央氣象署 (CWA) 開放資料平台。
- **功能**：
  - 抓取全台各縣市未來 12 小時的天氣預報（氣溫、降雨機率、天氣現象）。
  - 將縣市分為北、中、南、東、外島五大區塊整理顯示。
  - **AI 點評**：使用 Gemini AI 扮演「幽默氣象播報員」，提供今日重點、天氣觀察與穿搭建議。
- **發送方式**：Discord Webhook (Rich Embed)。

### 🌌 NASA 天文機器人 (`nasa_bot.py`)
- **資料來源**：NASA APOD API (若 API 失敗會自動切換為網頁爬蟲模式)。
- **功能**：
  - 抓取每日一張天文精選圖片 (Image of the Day)。
  - **AI 解說**：使用 Gemini AI 生成兩段內容：
    - **宇宙日記**：感性的第一人稱短文。
    - **天文科普**：白話文解釋天文現象。
  - 具備強大的容錯機制 (API 重試、爬蟲備援)。
- **發送方式**：Discord Webhook (Rich Embed)。

---

## 🛠️ 安裝與設定

### 1. 環境需求
- Python 3.11 或以上
- 建議使用虛擬環境

### 2. 安裝套件
請執行以下指令安裝所需套件：
```bash
pip install -r requirements.txt
```

### 3. 設定環境變數 (Secrets)
本專案依賴環境變數來管理金鑰，請在執行環境 (如 GitHub Actions Secrets 或本機 `.env`) 中設定以下變數：

| 變數名稱 | 說明 | 必填 | 取得方式 |
| :--- | :--- | :--- | :--- |
| `WEBHOOK_URL` | Discord Webhook 網址 | ✅ | Discord 頻道設定 -> 整合 -> Webhook |
| `GEMINI_API_KEY` | Google Gemini API Key | ✅ | [Google AI Studio](https://aistudio.google.com/) |
| `CWA_API_KEY` | 中央氣象署 API Key | ✅ | [氣象資料開放平台](https://opendata.cwa.gov.tw/) |
| `NASA_API_KEY` | NASA API Key | ❌ | [NASA APIs](https://api.nasa.gov/) (預設使用 `DEMO_KEY`) |

---

## 🚀 使用方法

### 本機執行
確保環境變數已設定後，可直接執行 Python 腳本：

**執行氣象機器人：**
```bash
python weather_bot.py
```

**執行 NASA 機器人：**
```bash
python nasa_bot.py
```

### GitHub Actions 自動化
本專案已包含 GitHub Actions 設定檔：
- **氣象廣播** (`WeatherBot.yml`)：預設為**每天台灣時間 06:00 (UTC 22:00)** 自動執行。
- **NASA 宇宙日記** (`nasa.yml`)：預設為**每天台灣時間 22:00 (UTC 14:00)** 自動執行。

若要啟用，請確保 GitHub Repository 的 **Settings -> Secrets and variables -> Actions** 中已設定好上述的環境變數。

---

## 📂 專案結構
```
TaiwanWeatherBot/
├── .github/workflows/
│   ├── WeatherBot.yml    # 台灣氣象廣播排程
│   └── nasa.yml          # NASA 宇宙日記排程
├── weather_bot.py        # 氣象機器人主程式
├── nasa_bot.py           # NASA 機器人主程式
├── requirements.txt      # Python 套件清單
├── .gitignore            # Git 忽略清單
├── LICENSE               # 授權文件
└── README.md             # 專案說明文件

```

---

## 🤝 如何貢獻 (Contributing)
歡迎提交 Issue 或 Pull Request 來改進這個專案！

1. Fork 這個專案
2. 建立您的 Feature Branch (`git checkout -b feature/AmazingFeature`)
3. 提交您的變更 (`git commit -m 'Add some AmazingFeature'`)
4. 推送到 Branch (`git push origin feature/AmazingFeature`)
5. 開啟 Pull Request

## 📝 授權 (License)
本專案採用 [MIT License](LICENSE) 授權。
